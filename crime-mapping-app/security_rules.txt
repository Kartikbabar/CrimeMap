rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic catch-all to allow everything during development
    // WARNING: Replace this with granular rules before production
    match /{document=**} {
      allow read, write: if true;
    }

    // Recommended Granular Rules (Optional - alternative to the above)
    /*
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    match /police/{userId} {
      allow read, write: if request.auth != null;
    }
    
    match /firs/{firId} {
      allow read, write: if request.auth != null;
    }
    
    match /patrols/{patrolId} {
      allow read, write: if request.auth != null;
    }
    
    match /personnel/{personnelId} {
      allow read, write: if request.auth != null;
    }
    */
  }
}
